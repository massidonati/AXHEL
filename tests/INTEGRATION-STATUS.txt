AXHEL TEST SUITE V5 — INTEGRATION STATUS

V5 has one mode only: deterministic extended regression.

Removed from V4:
- smoke target and runner
- exhaustive target/option and runner
- AXHEL_TEST_LEVEL matrix switching
- regression seed override

V5 regression includes the former V4 exhaustive matrix and extends it with:
- explicit 62-bit coverage
- all modulus bit-widths 20..62 for public Barrett-dispatch kernels
- direct SVE ShiftRight128LowPart<1..60> white-box coverage
- three deterministic q values for critical 50/52/59/60/61/62-bit classes
- SVE/unroll tail lengths around 1/2/3/4/7/8/16 vector lengths
- guard canaries around Add/Sub/Mul/FMA/Reduce result buffers and source checks
- complete selected NTT power-of-two degree set including 128/512/2048
- factor-4 forward and factor-2 inverse lazy-input checks on every NTT degree
- fixed deterministic PRNG stream implemented without uniform_int_distribution

The Native/SVE dual-build mechanism and test-only NTT SVE hooks remain unchanged.
